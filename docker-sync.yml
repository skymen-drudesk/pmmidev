version: "2"

options:
  verbose: true

syncs:
  pxi-unison-sync:
    src: './'
    dest: '/var/www'
    sync_userid: '82'
    sync_excludes: [
      '.git/',
      '.gitignore',
      '.DS_Store',
      '._.DS_Store',
      '.idea'
    ]
    sync_prefer: 'newer'
