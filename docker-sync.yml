version: "2"

options:
  verbose: true

syncs:
  pmmi-sync:
    notify_terminal: true
    src: './'
    sync_userid: '82'
    sync_excludes: [
      '.git/',
      '.gitignore',
      '.DS_Store',
      '._.DS_Store',
      '.idea'
    ]
    sync_strategy: 'unison'
    sync_prefer: 'newer'
    watch_excludes: [
      '.git/',
      '.gitignore',
      '.DS_Store',
      '._.DS_Store',
      '.idea'
    ]
