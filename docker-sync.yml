version: "2"

options:
  verbose: true

syncs:
  pmmi-unison-sync:
    src: './'
    dest: '/var/www'
    sync_userid: '82'
    sync_excludes: [
      '.git/',
      '.gitignore',
      '.DS_Store',
      '._.DS_Store',
      '.idea'
    ]
